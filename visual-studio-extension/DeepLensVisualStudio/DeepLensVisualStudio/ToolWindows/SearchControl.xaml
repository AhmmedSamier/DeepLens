<UserControl
    x:Class="DeepLensVisualStudio.ToolWindows.SearchControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:DeepLensVisualStudio.ToolWindows"
    xmlns:vsshell="clr-namespace:Microsoft.VisualStudio.Shell;assembly=Microsoft.VisualStudio.Shell.15.0"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:system="clr-namespace:System;assembly=mscorlib"
    d:DesignHeight="600"
    d:DesignWidth="900"
    Background="{DynamicResource {x:Static vsshell:VsBrushes.ToolWindowBackgroundKey}}"
    mc:Ignorable="d">

    <UserControl.Resources>
        <!--  Codicon Font Family  -->
        <FontFamily x:Key="CodiconFont">/DeepLensVisualStudio;component/Fonts/#codicon</FontFamily>

        <!--  String to Visibility Converter  -->
        <local:StringToVisibilityConverter x:Key="StringToVisibilityConverter" />
        <local:InverseStringToVisibilityConverter x:Key="InverseStringToVisibilityConverter" />
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />

        <!-- Colors replaced by VS Theme Brushes -->

        <!--  Tab Button Style  -->
        <Style x:Key="TabButtonStyle" TargetType="RadioButton">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border
                            x:Name="Border"
                            Margin="2,0"
                            Padding="16,8"
                            Background="Transparent"
                            BorderThickness="0"
                            CornerRadius="4,4,0,0">
                            <TextBlock
                                FontSize="13"
                                FontWeight="Medium"
                                Foreground="{DynamicResource {x:Static vsshell:VsBrushes.ToolWindowTextKey}}"
                                Text="{TemplateBinding Content}" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Border" Property="Background"
                                        Value="{DynamicResource {x:Static vsshell:VsBrushes.HighlightKey}}" />
                                <Setter TargetName="Border" Property="TextBlock.Foreground"
                                        Value="{DynamicResource {x:Static vsshell:VsBrushes.HighlightTextKey}}" />
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background"
                                        Value="{DynamicResource {x:Static vsshell:VsBrushes.ComboBoxBackgroundKey}}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!--  ListBox Item Style  -->
        <Style x:Key="ResultItemStyle" TargetType="ListBoxItem">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Padding" Value="12,8" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border
                            x:Name="Border"
                            Padding="{TemplateBinding Padding}"
                            Background="{TemplateBinding Background}">
                            <ContentPresenter />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Border" Property="Background"
                                        Value="{DynamicResource {x:Static vsshell:VsBrushes.HighlightKey}}" />
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background"
                                        Value="{DynamicResource {x:Static vsshell:VsBrushes.CommandBarHoverKey}}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ActionButtonStyle" TargetType="Button">
            <Setter Property="Padding" Value="4" />
            <Setter Property="Width" Value="28" />
            <Setter Property="Height" Value="28" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="FontFamily" Value="Segoe MDL2 Assets" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="Foreground" Value="{DynamicResource {x:Static vsshell:VsBrushes.ToolWindowTextKey}}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="2">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background"
                            Value="{DynamicResource {x:Static vsshell:VsBrushes.CommandBarHoverKey}}" />
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <!--  Main Container with Shadow  -->
    <Border
        Background="{DynamicResource {x:Static vsshell:VsBrushes.ToolWindowBackgroundKey}}"
        BorderBrush="{DynamicResource {x:Static vsshell:VsBrushes.ToolWindowBorderKey}}"
        BorderThickness="1"
        CornerRadius="8">
        <Border.Effect>
            <DropShadowEffect
                BlurRadius="20"
                Direction="270"
                Opacity="0.5"
                ShadowDepth="5"
                Color="Black" />
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!--  Tab Bar  -->
            <Border
                Grid.Row="0"
                Background="{DynamicResource {x:Static vsshell:VsBrushes.ToolWindowBackgroundKey}}"
                BorderBrush="{DynamicResource {x:Static vsshell:VsBrushes.ToolWindowBorderKey}}"
                BorderThickness="0,0,0,1"
                CornerRadius="8,8,0,0">
                <StackPanel Margin="8,8,8,0" Orientation="Horizontal">
                    <RadioButton
                        Content="All"
                        GroupName="SearchTabs"
                        IsChecked="{Binding FilterAll, Mode=TwoWay}"
                        Style="{StaticResource TabButtonStyle}" />
                    <RadioButton
                        Content="Classes"
                        GroupName="SearchTabs"
                        IsChecked="{Binding FilterClasses, Mode=TwoWay}"
                        Style="{StaticResource TabButtonStyle}" />
                    <RadioButton
                        Content="Symbols"
                        GroupName="SearchTabs"
                        IsChecked="{Binding FilterSymbols, Mode=TwoWay}"
                        Style="{StaticResource TabButtonStyle}" />
                    <RadioButton
                        Content="Files"
                        GroupName="SearchTabs"
                        IsChecked="{Binding FilterFiles, Mode=TwoWay}"
                        Style="{StaticResource TabButtonStyle}" />
                    <RadioButton
                        Content="Text"
                        GroupName="SearchTabs"
                        IsChecked="{Binding FilterText, Mode=TwoWay}"
                        Style="{StaticResource TabButtonStyle}" />
                    <RadioButton
                        Content="Endpoints"
                        GroupName="SearchTabs"
                        IsChecked="{Binding FilterEndpoints, Mode=TwoWay}"
                        Style="{StaticResource TabButtonStyle}" />
                </StackPanel>
            </Border>

            <!--  Search Input Area  -->
            <Border
                Grid.Row="1"
                Padding="12"
                Background="{DynamicResource {x:Static vsshell:VsBrushes.ComboBoxBackgroundKey}}"
                BorderBrush="{DynamicResource {x:Static vsshell:VsBrushes.ToolWindowBorderKey}}"
                BorderThickness="0,0,0,1">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <!--  Search Icon  -->
                    <Path
                        Grid.Column="0"
                        Width="16"
                        Height="16"
                        Margin="0,0,8,0"
                        VerticalAlignment="Center"
                        Data="M15.5 14h-.79l-.28-.27C15.41 12.59 16 11.11 16 9.5 16 5.91 13.09 3 9.5 3S3 5.91 3 9.5 5.91 16 9.5 16c1.61 0 3.09-.59 4.23-1.57l.27.28v.79l5 4.99L20.49 19l-4.99-5zm-6 0C7.01 14 5 11.99 5 9.5S7.01 5 9.5 5 14 7.01 14 9.5 11.99 14 9.5 14z"
                        Fill="{DynamicResource {x:Static vsshell:VsBrushes.ToolWindowTextKey}}"
                        Stretch="Uniform" />

<!--  Search TextBox  -->
                    <Grid Grid.Column="1" VerticalAlignment="Center">
                        <TextBlock
                            IsHitTestVisible="False"
                            Text="Search for classes, symbols, files, text, or endpoints"
                            VerticalAlignment="Center"
                            FontSize="14"
                            Foreground="{DynamicResource {x:Static vsshell:VsBrushes.GrayTextKey}}">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Visibility" Value="Collapsed" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Text, ElementName=SearchTextBox}" Value="">
                                            <Setter Property="Visibility" Value="Visible" />
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding Text, ElementName=SearchTextBox}" Value="{x:Null}">
                                            <Setter Property="Visibility" Value="Visible" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                        <TextBox
                            Name="SearchTextBox"
                            VerticalAlignment="Center"
                            Background="Transparent"
                            BorderThickness="0"
                            CaretBrush="{DynamicResource {x:Static vsshell:VsBrushes.ToolWindowTextKey}}"
                            FontSize="14"
                            Foreground="{DynamicResource {x:Static vsshell:VsBrushes.ToolWindowTextKey}}"
                            Text="{Binding SearchQuery, UpdateSourceTrigger=PropertyChanged}"
                            AutomationProperties.Name="Search query"
                            ToolTip="Search for classes, symbols, files, text, or endpoints" />
                    </Grid>

                    <!-- Watermark Overlay -->
                    <TextBlock
                        Grid.Column="1"
                        VerticalAlignment="Center"
                        Margin="4,0,0,0"
                        FontSize="{Binding FontSize, ElementName=SearchTextBox}"
                        Foreground="{DynamicResource {x:Static vsshell:VsBrushes.GrayTextKey}}"
                        IsHitTestVisible="False"
                        Text="Search for classes, symbols, files, text, or endpoints">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Visibility" Value="Collapsed" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding Text.Length, ElementName=SearchTextBox}" Value="0">
                                        <Setter Property="Visibility" Value="Visible" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>

                    <!--  Search TextBox Watermark  -->
                    <TextBlock
                        Grid.Column="1"
                        VerticalAlignment="Center"
                        Foreground="{DynamicResource {x:Static vsshell:VsBrushes.GrayTextKey}}"
                        FontSize="14"
                        IsHitTestVisible="False"
                        Text="Search for classes, symbols, files, text, or endpoints">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Visibility" Value="Hidden" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding ElementName=SearchTextBox, Path=Text}" Value="">
                                        <Setter Property="Visibility" Value="Visible" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>

                     <!-- Search Placeholder / Watermark -->
                     <TextBlock
                         Grid.Column="1"
                         IsHitTestVisible="False"
                         Margin="3,0,0,0"
                         VerticalAlignment="Center"
                         FontSize="14"
                         Foreground="{DynamicResource {x:Static vsshell:VsBrushes.GrayTextKey}}"
                         Text="Search for classes, symbols, files, text, or endpoints">
                         <TextBlock.Style>
                             <Style TargetType="TextBlock">
                                 <Setter Property="Visibility" Value="Collapsed" />
                                 <Style.Triggers>
                                     <DataTrigger Binding="{Binding Text, ElementName=SearchTextBox}" Value="">
                                         <Setter Property="Visibility" Value="Visible" />
                                     </DataTrigger>
                                 </Style.Triggers>
                             </Style>
                         </TextBlock.Style>
                     </TextBlock>

                     <TextBlock
                         Grid.Column="1"
                         VerticalAlignment="Center"
                         Margin="4,0,0,0"
                         Foreground="{DynamicResource {x:Static vsshell:VsBrushes.GrayTextKey}}"
                         FontSize="14"
                         IsHitTestVisible="False"
                         Text="Search for classes, symbols, files, text, or endpoints"
                         Visibility="{Binding SearchQuery, Converter={StaticResource InverseStringToVisibilityConverter}}" />

                    <Button
                        x:Name="ClearSearchButton"
                        Grid.Column="2"
                        Width="24"
                        Height="24"
                        Margin="4,0,0,0"
                        VerticalAlignment="Center"
                        Background="Transparent"
                        BorderThickness="0"
                        Click="ClearSearchButton_Click"
                        Cursor="Hand"
                        FontFamily="Segoe MDL2 Assets"
                        FontSize="12"
                        Foreground="{DynamicResource {x:Static vsshell:VsBrushes.ToolWindowTextKey}}"
                        Content="&#xE711;"
                        ToolTip="Clear search"
                        AutomationProperties.Name="Clear search"
                        Visibility="{Binding SearchQuery, Converter={StaticResource StringToVisibilityConverter}}" />
                </Grid>
            </Border>

            <!--  Results List  -->
            <ListBox
                x:Name="ResultsList"
                Grid.Row="2"
                Margin="10,5,10,0"
                AutomationProperties.Name="Search Results"
                Background="{DynamicResource {x:Static vsshell:VsBrushes.ToolWindowBackgroundKey}}"
                BorderThickness="0"
                ItemContainerStyle="{StaticResource ResultItemStyle}"
                ItemsSource="{Binding Results}"
                ScrollViewer.CanContentScroll="True"
                ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                ScrollViewer.VerticalScrollBarVisibility="Auto"
                SelectedItem="{Binding SelectedResult}"
                VirtualizingPanel.IsVirtualizing="True"
                VirtualizingPanel.VirtualizationMode="Recycling">

                <!-- Design-time sample data -->
                <d:ListBox.ItemsSource>
                    <system:Collections.ArrayList>
                        <local:SearchResultViewModel
                            Name="UserController"
                            Kind="Class"
                            FilePath="C:\Projects\MyApp\Controllers\UserController.cs"
                            RelativePath="Controllers\UserController.cs"
                            Line="15"
                            ContainerName="MyApp.Controllers" />
                        <local:SearchResultViewModel
                            Name="GetUserById"
                            Kind="Method"
                            FilePath="C:\Projects\MyApp\Controllers\UserController.cs"
                            RelativePath="Controllers\UserController.cs"
                            Line="42"
                            ContainerName="UserController" />
                        <local:SearchResultViewModel
                            Name="IUserService"
                            Kind="Interface"
                            FilePath="C:\Projects\MyApp\Services\IUserService.cs"
                            RelativePath="Services\IUserService.cs"
                            Line="8"
                            ContainerName="MyApp.Services" />
                        <local:SearchResultViewModel
                            Name="UserName"
                            Kind="Property"
                            FilePath="C:\Projects\MyApp\Models\User.cs"
                            RelativePath="Models\User.cs"
                            Line="23"
                            ContainerName="User" />
                        <local:SearchResultViewModel
                            Name="appsettings.json"
                            Kind="File"
                            FilePath="C:\Projects\MyApp\appsettings.json"
                            RelativePath="appsettings.json"
                            Line="1"
                            ContainerName="" />
                        <local:SearchResultViewModel
                            Name="/api/users/{id}"
                            Kind="Endpoint"
                            FilePath="C:\Projects\MyApp\Controllers\UserController.cs"
                            RelativePath="Controllers\UserController.cs"
                            Line="40"
                            ContainerName="UserController.GetUserById" />
                    </system:Collections.ArrayList>
                </d:ListBox.ItemsSource>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid Margin="4,6">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="24" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>

                            <!--  Icon  -->
                            <TextBlock
                                x:Name="IconTextBlock"
                                Grid.Row="0"
                                Grid.RowSpan="1"
                                Grid.Column="0"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                FontFamily="{StaticResource CodiconFont}"
                                FontSize="16"
                                Foreground="{DynamicResource {x:Static vsshell:VsBrushes.ToolWindowTextKey}}"
                                Text="{Binding KindIcon}" />

                            <!--  Name and Partial Path  -->
                            <StackPanel
                                Grid.Row="0"
                                Grid.Column="1"
                                Margin="6,0,0,0"
                                VerticalAlignment="Center"
                                Orientation="Horizontal">
                                <TextBlock
                                    x:Name="NameTextBlock"
                                    Margin="0,0,8,0"
                                    FontSize="14"
                                    Foreground="{DynamicResource {x:Static vsshell:VsBrushes.ToolWindowTextKey}}"
                                    Text="{Binding Name}" />

                                <TextBlock
                                    x:Name="DirectoryPathTextBlock"
                                    VerticalAlignment="Bottom"
                                    FontSize="13"
                                    Foreground="{DynamicResource {x:Static vsshell:VsBrushes.GrayTextKey}}"
                                    Text="{Binding DirectoryPath}"
                                    TextTrimming="CharacterEllipsis" />
                            </StackPanel>

                            <!--  Full Path (Second Line)  -->
                            <TextBlock
                                x:Name="RelativePathTextBlock"
                                Grid.Row="1"
                                Grid.Column="1"
                                Margin="6,4,0,0"
                                FontSize="12"
                                Foreground="{DynamicResource {x:Static vsshell:VsBrushes.GrayTextKey}}"
                                Text="{Binding RelativePath}"
                                TextTrimming="CharacterEllipsis"
                                Visibility="{Binding RelativePath, Converter={StaticResource StringToVisibilityConverter}}" />

                            <!--  Actions (Hover)  -->
                            <StackPanel
                                x:Name="ActionPanel"
                                Grid.Row="0"
                                Grid.RowSpan="2"
                                Grid.Column="2"
                                HorizontalAlignment="Right"
                                VerticalAlignment="Center"
                                Orientation="Horizontal"
                                Visibility="Hidden">
                                <Button
                                    Command="{Binding CopyPathCommand}"
                                    Content="&#xE8C8;"
                                    AutomationProperties.Name="Copy Path"
                                    Style="{StaticResource ActionButtonStyle}"
                                    ToolTip="Copy Path" />
                                <Button
                                    Command="{Binding RevealInExplorerCommand}"
                                    Content="&#xE838;"
                                    AutomationProperties.Name="Open Folder"
                                    Style="{StaticResource ActionButtonStyle}"
                                    ToolTip="Open Folder" />
                            </StackPanel>

                        </Grid>
                        <DataTemplate.Triggers>
                            <!--  Show actions on hover OR selection  -->
                            <DataTrigger
                                Binding="{Binding IsMouseOver, RelativeSource={RelativeSource AncestorType=ListBoxItem}}"
                                Value="True">
                                <Setter TargetName="ActionPanel" Property="Visibility" Value="Visible" />
                            </DataTrigger>
                            <DataTrigger
                                Binding="{Binding IsSelected, RelativeSource={RelativeSource AncestorType=ListBoxItem}}"
                                Value="True">
                                <Setter TargetName="ActionPanel" Property="Visibility" Value="Visible" />
                                <Setter TargetName="IconTextBlock" Property="Foreground"
                                        Value="{DynamicResource {x:Static vsshell:VsBrushes.HighlightTextKey}}" />
                                <Setter TargetName="NameTextBlock" Property="Foreground"
                                        Value="{DynamicResource {x:Static vsshell:VsBrushes.HighlightTextKey}}" />
                                <Setter TargetName="DirectoryPathTextBlock" Property="Foreground"
                                        Value="{DynamicResource {x:Static vsshell:VsBrushes.HighlightTextKey}}" />
                                <Setter TargetName="RelativePathTextBlock" Property="Foreground"
                                        Value="{DynamicResource {x:Static vsshell:VsBrushes.HighlightTextKey}}" />
                            </DataTrigger>
                        </DataTemplate.Triggers>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <!-- Empty State Message -->
            <StackPanel
                Grid.Row="2"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Visibility="{Binding ShowEmptyState, Converter={StaticResource BooleanToVisibilityConverter}}">
                <TextBlock
                    Text="No results found"
                    FontSize="16"
                    FontWeight="SemiBold"
                    Foreground="{DynamicResource {x:Static vsshell:VsBrushes.GrayTextKey}}"
                    HorizontalAlignment="Center" />
                <TextBlock
                    Text="Try adjusting your filters or search term."
                    FontSize="13"
                    Foreground="{DynamicResource {x:Static vsshell:VsBrushes.GrayTextKey}}"
                    HorizontalAlignment="Center"
                    Margin="0,4,0,0" />
            </StackPanel>

            <!--  Status Bar (Sticky at Bottom)  -->
            <Border
                Grid.Row="3"
                Padding="12,8"
                Background="{DynamicResource {x:Static vsshell:VsBrushes.ToolWindowBackgroundKey}}"
                BorderBrush="{DynamicResource {x:Static vsshell:VsBrushes.ToolWindowBorderKey}}"
                BorderThickness="0,1,0,0">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <TextBlock
                        Grid.Column="0"
                        HorizontalAlignment="Left"
                        VerticalAlignment="Center"
                        FontSize="11"
                        Foreground="{DynamicResource {x:Static vsshell:VsBrushes.GrayTextKey}}"
                        Text="{Binding StatusText}" />

                    <StackPanel
                        Grid.Column="1"
                        Orientation="Horizontal"
                        Visibility="{Binding IsIndexing, Converter={StaticResource BooleanToVisibilityConverter}}">

                        <TextBlock
                            Margin="0,0,10,0"
                            VerticalAlignment="Center"
                            FontSize="11"
                            Foreground="{DynamicResource {x:Static vsshell:VsBrushes.GrayTextKey}}"
                            Text="{Binding IndexingStatusText}" />

                        <ProgressBar
                            AutomationProperties.Name="Indexing progress"
                            Width="100"
                            Height="10"
                            Maximum="100"
                            Minimum="0"
                            AutomationProperties.Name="Indexing progress"
                            Value="{Binding IndexingProgress}" />
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
    </Border>
</UserControl>
