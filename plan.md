1. Add an entry to `.jules/bolt.md` detailing the learning that function call overheads inside hot loops should be avoided by hoisting bitflag checks and inlining function logic if needed, with `// eslint-disable-next-line sonarjs/cognitive-complexity` used to bypass complexity warnings. (Already largely handled in memory rules).
2. Complete pre commit steps to ensure proper testing, verification, review, and reflection are done.
3. Submit the change with title `⚡ Bolt: [performance improvement]`.
